# marketplaces

Сервис-интегратор маркетплейсов (плагин-расширение).

## Запуск

Поднимается через корневой `docker-compose.yml` вместе с остальными сервисами.

## API

- `GET /health` — healthcheck


